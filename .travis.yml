language: python

matrix:
    include:
        - python: 3.6.1
          env: TOXENV=py36,coverage-ci

        - python: 3.7
          env: TOXENV=py37,coverage-ci

install:
    - pip install tox

script:
    - tox
